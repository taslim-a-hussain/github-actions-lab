name: Issue Comment

on:
    issues:
        types:
            - opened
            - reopened

jobs:
    comment:
        runs-on: ubuntu-latest
        steps:
            - name: Dump GitHub context
              run: echo '${{ toJSON(github.event) }}' | jq
              shell: bash
